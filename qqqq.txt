<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup Redirect URLs</title>
    <style>
        body {
            background: linear-gradient(135deg, #ff007f, #0073ff);
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .form-container {
            background-color: white;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 350px;
        }
        input[type="text"], input[type="button"] {
            width: calc(100% - 22px);
            padding: 10px;
            margin: 10px auto;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 16px;
            display: block;
        }
        input[type="button"] {
            background-color: #66a6ff;
            color: white;
            cursor: pointer;
            border: none;
        }
        input[type="button"]:hover {
            background-color: #89f7fe;
        }
        h1 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
        }
        .settings-links {
            margin-top: 20px;
        }
        .url-list {
            margin-top: 20px;
            text-align: left;
        }
        .url-item {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        .copy-btn {
            margin-right: 10px;
            padding: 5px 10px;
            background-color: #66a6ff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .copy-btn:hover {
            background-color: #89f7fe;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Set Redirect URLs</h1>
        <input type="text" id="redirectUrl1" placeholder="Enter Redirect URL 1">
        <input type="text" id="redirectUrl2" placeholder="Enter Redirect URL 2">
        <input type="text" id="redirectUrl3" placeholder="Enter Redirect URL 3">
        <input type="button" value="Submit" id="submitUrls">

        <div class="settings-links">
            <button onclick="window.location.href='index.html'">Back to Main Page</button>
        </div>

        <div class="url-list">
            <h2>Current Redirect URLs:</h2>
            <div class="url-item">
                <button class="copy-btn" onclick="copyRedirectLink(1)">Copy URL 1</button>
                <p><strong>URL 1:</strong> <span id="displayUrl1">Not Set</span></p>
            </div>
            <div class="url-item">
                <button class="copy-btn" onclick="copyRedirectLink(2)">Copy URL 2</button>
                <p><strong>URL 2:</strong> <span id="displayUrl2">Not Set</span></p>
            </div>
            <div class="url-item">
                <button class="copy-btn" onclick="copyRedirectLink(3)">Copy URL 3</button>
                <p><strong>URL 3:</strong> <span id="displayUrl3">Not Set</span></p>
            </div>
        </div>
    </div>

    <script>
        function updateUrlDisplay() {
            document.getElementById('displayUrl1').textContent = localStorage.getItem('redirectUrl1') || 'Not Set';
            document.getElementById('displayUrl2').textContent = localStorage.getItem('redirectUrl2') || 'Not Set';
            document.getElementById('displayUrl3').textContent = localStorage.getItem('redirectUrl3') || 'Not Set';
        }

        updateUrlDisplay();

        document.getElementById('submitUrls').addEventListener('click', function() {
            const url1 = document.getElementById('redirectUrl1').value;
            const url2 = document.getElementById('redirectUrl2').value;
            const url3 = document.getElementById('redirectUrl3').value;

            localStorage.setItem('redirectUrl1', url1);

localStorage.setItem('redirectUrl2', url2);
            localStorage.setItem('redirectUrl3', url3);

            alert('Redirect URLs set successfully!');
            updateUrlDisplay();
        });

        function copyRedirectLink(urlNumber) {
            let redirectUrl;
            switch (urlNumber) {
                case 1:
                    redirectUrl = localStorage.getItem('redirectUrl1');
                    break;
                case 2:
                    redirectUrl = localStorage.getItem('redirectUrl2');
                    break;
                case 3:
                    redirectUrl = localStorage.getItem('redirectUrl3');
                    break;
                default:
                    redirectUrl = null;
            }

            if (redirectUrl) {
                const fullUrl = ${window.location.origin}/index.html?redirect=${encodeURIComponent(redirectUrl)};
                navigator.clipboard.writeText(fullUrl).then(function() {
                    alert(`Copied URL ${urlNumber} to clipboard: ${fullUrl}`);
                }, function() {
                    alert('Failed to copy URL.');
                });
            } else {
                alert(`URL ${urlNumber} is not set.`);
            }
        }
    </script>
</body>
</html>